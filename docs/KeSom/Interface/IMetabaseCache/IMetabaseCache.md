# IMetabaseCache

IMetabaseCache
-


# IMetabaseCache


## Описание


Интерфейс IMetabaseCache содержит
 свойства и методы для работы с кешем репозитория.


## Комментарии


Доступ к кешу репозитория возвращает свойство [IMetabase.Cache](../IMetabase/IMetabase.Cache.htm).


При работе с объектами для ускорения доступа к их данным реализовано
 кеширование данных в кеше репозитория. При кешировании на компьютере пользователя
 создаются временные копии объекта, содержащие часто используемые данные.
 Если объект имеет параметры, то копии создаются для каждого набора параметров,
 с которыми открывается объект. Кеширование данных поддерживают такие объекты,
 как: таблицы, справочники, базы данных временных рядов, глобальные переменные.


Также в кеше репозитория хранятся ссылки на используемые объекты. Ссылка
 содержит адрес объекта в памяти, но не его данные или метаданные. Ссылки
 используются для организации взаимодействия с объектами в ядре платформы
 и при работе с объектами из прикладного кода.


Кеш репозитория хранится и используется в рамках одного соединения с
 репозиторием.


## Свойства


		 Имя свойства
		 Краткое описание


		 ![](../../Property_Image.gif)
		 [SharedValue](IMetabaseCache.SharedValue.htm)
		 Свойство SharedValue
		 возвращает текущее значение глобальной переменной.


## Методы


		 Имя метода
		 Краткое описание


		 ![](../../Sub_Image.gif)
		 [Flush](IMetabaseCache.Flush.htm)
		 Метод Flush очищает
		 ссылки на объект репозитория по его ключу.


		 ![](../../Sub_Image.gif)
		 [FlushAll](IMetabaseCache.FlushAll.htm)
		 Метод FlushAll очищает
		 ссылки на объекты, хранящиеся в кеше репозитория.


		 ![](../../Sub_Image.gif)
		 [FlushById](IMetabaseCache.FlushById.htm)
		 Метод FlushById очищает
		 ссылки на объект репозитория по его идентификатору.


		 ![](../../Sub_Image.gif)
		 [FlushInstance](IMetabaseCache.FlushInstance.htm)
		 Метод FlushInstance
		 очищает кеш объекта репозитория по его ключу и заданным значениям
		 параметров.


		 ![](../../Sub_Image.gif)
		 [FlushInstanceById](IMetabaseCache.FlushInstanceById.htm)
		 Метод FlushInstanceById
		 очищает кеш объекта репозитория по его идентификатору и заданным
		 значениям параметров.


		 ![](../../Sub_Image.gif)
		 [GetInstance](IMetabaseCache.GetInstance.htm)
		 Метод GetInstance возвращает
		 экземпляр объекта из кеша репозитория по ключу и указанному набору
		 параметров.


		 ![](../../Sub_Image.gif)
		 [GetInstanceById](IMetabaseCache.GetInstanceById.htm)
		 Метод GetInstanceById
		 возвращает экземпляр объекта из кеша репозитория по идентификатору
		 и указанному набору параметров.


		 ![](../../Sub_Image.gif)
		 [MetabaseFlush](IMetabaseCache.MetabaseFlush.htm)
		 Метод MetabaseFlush
		 осуществляет очистку кеша объекта репозитория без очистки кеша
		 ссылок на объекты сборок и их зависимостей.


		 ![](../../Sub_Image.gif)
		 [MetabaseFlushAll](IMetabaseCache.MetabaseFlushAll.htm)
		 Метод MetabaseFlushAll
		 осуществляет очистку кеша ссылок на объекты сборок и их зависимостей.


См. также:


[Интерфейсы сборки Metabase](../KeSom_Interface.htm)


		Справочная
		 система на версию 10.9
		 от 18/08/2025,
		 © ООО «ФОРСАЙТ»,
