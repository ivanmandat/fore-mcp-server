# ITabSheet.BeginUpdate

ITabSheet.BeginUpdate
-


# ITabSheet.BeginUpdate


## Синтаксис


BeginUpdate([Flags: [TabUpdateFlags](../../Enums/TabUpdateFlags.htm)
 = 0]);


## Параметры


Flags. Режим обновления таблицы.


## Описание


Метод BeginUpdate отключает
 часть функций таблицы для экономии времени, связанного с изменением и
 обработкой данных таблицы.


## Комментарии


После вызова данного метода события будут срабатывать следующим образом:


	- при первом изменении значения в какой-либо ячейке
	 один раз произойдет событие OnBeforeCellsChange.


	- при первом изменении выделенной ячейки (выделенного
	 диапазона ячеек) один раз произойдет событие OnBeginSelectionChange.


При дальнейшем изменении значений, будут срабатывать события, связанные
 с изменением значений в конкретных ячейках (OnBeforeCellChange, OnCellChange,
 OnChangeCellContent). События, связанные с изменением выделенного
 диапазона ячеек, срабатывать не будут. При внесении изменений в таблицу
 ведётся [стек изменений](ITabSheet.UndoRedo.htm), на формирование
 которого дополнительно тратятся ресурсы системы. Используя параметр Flags, можно временно отключить
 формирование данного стека.


После вызова метода [EndUpdate](ITabSheet.EndUpdate.htm)
 один раз произойдут события OnCellsChange, OnSelectionChange и OnEndSelectionChange.


См. также:


[ITabSheet](ITabSheet.htm)


		Справочная
		 система на версию 10.9
		 от 18/08/2025,
		 © ООО «ФОРСАЙТ»,
