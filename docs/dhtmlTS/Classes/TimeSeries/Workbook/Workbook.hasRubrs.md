# Workbook.hasRubrs

Workbook.hasRubrs
-


# Workbook.hasRubrs


## Синтаксис


hasRubrs ();


## Описание


Метод hasRubrs определяет, является
 ли источник рабочей книги базой данных временных рядов.


## Комментарии


Метод возвращает true, если
 в источнике рабочей книги есть база данных временных рядов, иначе метод
 возвращает false.


## Пример


Для выполнения примера предполагается наличие на странице компонента
 [WorkbookBox](../../../Components/TimeSeries/WorkbookBox/WorkbookBox.htm)
 с наименованием «workbookBox» (см. «[Пример
 создания компонента WorkbookBox](../../../Components/TimeSeries/WorkbookBox/Component_WorkbookBox.htm)»), также необходимо в обработчике
 события открытия документа добавить следующий код:


	var source = workbookBox.getSource().getActiveSheet();//возвращаем экземпляр класса PP.TS.Workbook
	//Получаем признак наличия базы данных временных рядов у рабочей книги
	var hasRubrs = source.hasRubrs();
	if(hasRubrs)
	alert("Источником указанной рабочей книги является база данных временных рядов");
	else
	alert("Источником указанной рабочей книги не является база данных временных рядов");

После выполнения примера на html-странице будет размещен компонент [WorkbookBox](../../../Components/TimeSeries/WorkbookBox/WorkbookBox.htm)
 и будет выведено сообщение о том, будет ли источник указанной рабочей
 книги является базой данных временных рядов.


См. также:


[Workbook](Workbook.htm)


		Справочная
		 система на версию 10.9
		 от 18/08/2025,
		 © ООО «ФОРСАЙТ»,
