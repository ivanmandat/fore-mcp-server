# Decimal.Parse

Decimal.Parse
-


# Decimal.Parse


## Синтаксис


Parse(Value: String): Decimal;


## Параметры


Value. Строковое значение,
 которое необходимо преобразовать к типу Decimal.


## Описание


Метод Parse преобразует строковое
 значение к типу Decimal без проверки на корректность строки.


## Комментарии


Данный метод не учитывает установленные региональные параметры. В значении,
 передаваемом в параметре Value,
 для разделения целой и дробной части должен использоваться символ «.», разделитель разрядов не используется.


Если преобразование не удалось, то генерируется исключительная ситуация.


## Пример


	Sub UserProc;

	Var

	    d: Decimal;

	    s: String;

	Begin

	    s := "12345.6789";

	    d := Decimal.Parse(s);

	End Sub UserProc;


После выполнения примера в переменной «d» будет содержаться значение
 «12345,6789».


См. также:


[Decimal](Decimal.htm)


		Справочная
		 система на версию 10.9
		 от 18/08/2025,
		 © ООО «ФОРСАЙТ»,
