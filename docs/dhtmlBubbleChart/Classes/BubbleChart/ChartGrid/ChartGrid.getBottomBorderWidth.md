# ChartGrid.getBottomBorderWidth

ChartGrid.getBottomBorderWidth
-


# ChartGrid.getBottomBorderWidth


## Синтаксис


getBottomBorderWidth();


## Описание


Метод getBottomBorderWidth возвращает
 ширину нижней границы координатной сетки пузырьковой диаграммы.


## Комментарии


Метод возвращает значение типа Number.


## Пример


Для выполнения примера необходимо наличие на html-странице компонента
 [BubbleChart](../../../Components/BubbleChart/BubbleChart.htm)
 с наименованием «bubbleChart» (см. «[Пример
 создания компонента BubbleChart](../../../Components/BubbleChart/BubbleChart_Example.htm)»). Получим ширину границ координатной
 сетки:


// Получаем координтаную сетку пузырьковой диаграммы
var grid = bubbleChart.getChartArea().getGrid();
// Получаем ширину нижней границы координатной сетки
console.log("Ширина нижней границы координатной сетки: " + grid.getBottomBorderWidth());
// Получаем ширину верхней границы координатной сетки
console.log("Ширина верхней границы координатной сетки: " + grid.getTopBorderWidth());
// Получаем ширину левой границы координатной сетки
console.log("Ширина левой границы координатной сетки: " + grid.getLeftBorderWidth());
// Получаем ширину правой границы координатной сетки
console.log("Ширина правой границы координатной сетки: " + grid.getRightBorderWidth());
В результате в консоль будут выведены значения ширины границ координатной
 сетки:


Ширина нижней границы координатной сетки: 4


Ширина верхней границы координатной сетки: 0


Ширина левой границы координатной сетки: 0


Ширина правой границы координатной сетки: 0


См. также:


[ChartGrid](ChartGrid.htm)


		Справочная
		 система на версию 10.9
		 от 18/08/2025,
		 © ООО «ФОРСАЙТ»,
