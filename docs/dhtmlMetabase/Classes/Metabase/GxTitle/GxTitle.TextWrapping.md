# GxTitle.TextWrapping

GxTitle.TextWrapping
-


# GxTitle.TextWrapping


## Синтаксис


TextWrapping: Boolean


## Описание


Свойство TextWrapping определяет
 возможность переноса текста заголовка.


## Комментарии


Допустимые значения:


	- true.
	 Если текст заголовка не умещается на одной строке, то он будет перенесен
	 на следующую строку (по умолчанию);


	- false.
	 Текст заголовка не будет переноситься.


Значение свойства устанавливается из JSON и с помощью метода setTextWrapping,
 а возвращается с помощью метода getTextWrapping.


## Пример


Для выполнения примера необходимо наличие на html-странице компонента
 [ExpressBox](dhtmlExpress.chm::/Components/Express/ExpressBox/ExpressBox.htm)
 с наименованием «expressBox» (см. «[Пример
 создания компонента ExpressBox](dhtmlExpress.chm::/Components/Express/ExpressBox/ExpressBox_Example.htm)»). Для появления заголовка необходимо
 нажать на кнопку «Заголовок» на вкладке «Главная». Проверим использование
 переноса текста заголовка:


// Получаем заголовок
var title = expressBox.getDataView().getTitleView();
// Проверяем видимость заголовка
if (title.getTextWrapping()) {
    console.log("Используется перенос текста заголовка");
} else {
    console.log("Перенос текста заголовка не используется");
}
В результате в консоль будет выведен результат проверки использования
 переноса текста заголовка:


Используется перенос текста заголовка


См. также:


[GxTitle](GxTitle.htm)


		Справочная
		 система на версию 10.9
		 от 18/08/2025,
		 © ООО «ФОРСАЙТ»,
